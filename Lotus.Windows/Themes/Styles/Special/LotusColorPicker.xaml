<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="clr-namespace:Lotus.Windows"
    xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Lotus.Windows;component/Themes/Styles/LotusResourceStyle.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--  Ширина прямоугольника предпросмотра цвета  -->
    <sys:Double x:Key="LotusColorPickerPreviewWidth">50</sys:Double>

    <!--  Выбор цвета по компонентам RGB  -->
    <Style
        BasedOn="{StaticResource {x:Type UserControl}}"
        TargetType="{x:Type ctrl:LotusColorPicker}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ctrl:LotusColorPicker}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Slider
                            Grid.Row="0"
                            Margin="{TemplateBinding Padding}"
                            Maximum="255"
                            Minimum="0"
                            Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Red, Mode=TwoWay}" />
                        <Slider
                            Grid.Row="1"
                            Margin="{TemplateBinding Padding}"
                            Maximum="255"
                            Minimum="0"
                            Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Green, Mode=TwoWay}" />
                        <Slider
                            Grid.Row="2"
                            Margin="{TemplateBinding Padding}"
                            Maximum="255"
                            Minimum="0"
                            Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Blue, Mode=TwoWay}" />
                        <Rectangle
                            Grid.RowSpan="3"
                            Grid.Column="1"
                            Width="{StaticResource LotusColorPickerPreviewWidth}"
                            Fill="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Color, Converter={StaticResource ColorToBrushConverter}}"
                            Stroke="Black"
                            StrokeDashArray="2 1"
                            StrokeThickness="1" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
